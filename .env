# Base local defaults (safe to commit; no secrets)

# App environment
ENV=local

# HTTP port for local-main.go
PORT=3000

# Log level: debug|info|warn|error
LOG_LEVEL=info

# AWS region for SDK clients (if added later)
AWS_REGION=eu-west-3

